# 環境構築

```bash
# yarnが入っているかの確認
yarn -v
# yarn入ってない場合
npm install -g yarn
```

```bash
# クローン(最初の一回のみ)
git clone https://github.com/GiikuCAMPvol1/frontend.git

# 最新のパッケージインストール
yarn install

# 開発サーバー起動
yarn dev
```